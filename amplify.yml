version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@10.33.0
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - ~/.local/share/pnpm/store/**/*
